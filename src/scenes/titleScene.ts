import Phaser from "phaser";

export default class titleScene extends Phaser.Scene {

    constructor() {
        super({ key: "titleScene" });
    }

    preload() {
    }

    create() {
    }

    update() {
    }
}
